'use strict';

/**
 * currency-chart-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::currency-chart-config.currency-chart-config');
