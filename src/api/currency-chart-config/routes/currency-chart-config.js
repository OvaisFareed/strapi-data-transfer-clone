'use strict';

/**
 * currency-chart-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::currency-chart-config.currency-chart-config');
