'use strict';

/**
 * currency-conversion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::currency-conversion.currency-conversion');
